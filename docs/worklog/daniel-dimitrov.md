# Worklog — Daniel Dimitrov

## [2026-07-15 00:45] T17 — Demo mode + edge-case UX
- Dev: daniel-dimitrov · Model: Opus 4.8 (1M context) · Branch: feat/T17-demo-edge-ux
- Done: (1) One-click demo mode — activated the Setup "Load demo" button → `scenarioStore.loadDemo()` applies `DEMO_CONFIG` (`src/fixtures/demoConfig.ts`) and generates, then jumps to Planning. **Found DEMO_SEED** with a dev script `scripts/findDemoSeed.ts` (loops seeds through the real generator + optimizer, keeps only those meeting every §Demo Mode criterion). Ran it over 3000 seeds: 2262 qualified; picked `demo-1` — 2 trips, 2 deferred, 2 side-door shops, all 6 cargo categories, 0 permanent unplaceables. README + report footer cite the seed. (2) Edge-case UX: global toast primitive (`toastStore` + `<Toaster>`, no lib) with a "Plan needs N trips" toast fired from the optimize-success path; Planning empty-state ("Nothing to deliver" card + Optimize disabled with tooltip when 0 requested units); pre-warning amber chip on shop cards ("N items won't fit this vehicle") computed by reusing T07's now-exported `prefilterPermanents`; preferred-side-door-unavailable → door chip strikethrough + "→ rear"; Simulation "Nothing could be loaded" overlay when no trip has placements; report red banner "Internal validation failed (n)" via defensive `validateLoad` re-check (console detail); NaN-safe percents already via T16's `fmtPctSafe` (added a zero-weight test); `ErrorBoundary` around the 3D canvas → "3D view failed to load" card with a reset button; `?debugOptimizerError` URL flag (`utils/debug.ts`) to exercise the retry-able error alert. 321 tests green (13 new).
- Files: src/fixtures/demoConfig.ts, scripts/findDemoSeed.ts, src/state/{scenarioStore,optimizationStore,toastStore}.ts, src/components/{Toaster,ErrorBoundary}.tsx, src/components/screens/{ScreenSetup(+test),ScreenPlanning(+test),ScreenSimulation,ScreenReport}.tsx, src/components/planning/ShopCard.tsx(+test), src/components/report/nanSafe.test.tsx, src/state/demoAndToast.test.ts, src/utils/debug.ts, src/features/optimizer/optimize.ts (export prefilterPermanents), src/App.tsx, README.md, docs/TASKS.md
- Decisions/deviations:
  - **Exported `prefilterPermanents` from `src/features/optimizer/optimize.ts`** (Track A file, additive export only — no logic change) so the planning pre-warning reuses the exact fit/payload check instead of reimplementing it, as the prompt directs. Flagging the cross-track touch here.
  - Demo criteria all satisfied strictly — no relaxation needed (the documented fallback order mixed-categories → side-door was not triggered).
  - Toast timers live in the `<Toaster>` host, not the store, so the store stays a plain data holder; `show()` dedupes identical live toasts to survive StrictMode double-fire.
  - Multi-trip toast fires from `optimizationStore.run().then` (centralized) rather than a screen effect, so it works regardless of which screen you're on when the run finishes.
  - The "Internal validation failed" banner re-runs `validateLoad` with `DEFAULT_OPTIMIZER_CONFIG` (the config the worker uses); it should always be 0 — it's a loud safety net, not an expected path.
  - `?debugOptimizerError` is the debug flag the prompt asked for to verify the error alert; it short-circuits `run()` into the error state (Retry stays in error while the flag is set — expected).
  - Manual checklist (Playwright, real optimizer, seed demo-1): Load demo → Planning shows `demo-1` → Optimize → 2 trip tabs + "Plan needs 2 trips" toast → Simulation renders (0 console errors) → Report NaN-free, footer cites demo-1, left side door in use. `?debugOptimizerError` → "Optimization failed: Debug…" alert + Retry button. Empty-state, pre-warning chip, door fallback, NaN-safety covered by RTL.
- Follow-ups: "Nothing could be loaded" overlay + report validation banner are hard to trigger from generated data (optimizer never emits invalid/empty loads) — T18's edge-case fixtures are the natural place to exercise them for real. Chunk-size build warning is pre-existing (T20 polish / code-splitting).

## [2026-07-15 00:05] T16 — Report screen, trip selector, shop legend
- Dev: daniel-dimitrov · Model: Opus 4.8 (1M context) · Branch: feat/T16-report-screen
- Done: full Optimization Report + two shared components. Pure presentation over T08's `buildReportModel` — no metric math in the UI. **Shared** (`src/components/simulation/`): `TripSelector` (tabs bound to `uiStore.selectedTripId`, micro-summary `N stops · M units · weight%` per tab, first-trip fallback when unselected) and `ShopLegend` (color dot + stop # + name per stop in the selected trip; click toggles `uiStore.shopFilter` → dims 3D cargo per T13; clear-filter affordance; ⚠ badge for shops with deferred cargo, plus an `alsoWarnShopIds` prop the report/sim passes for unplaceable shops). Colors come from `shopColorById` — the same source the 3D `CargoBox` uses, so legend dots always match boxes. **Report** (`src/components/report/`): overall header (color-graded `ScoreBadge`, vehicle, trips, `SeedBadge`, elapsedMs, `ExportButtons` for Scenario/Result JSON via T03 `downloadJson`), selected-trip view driven by `TripSelector`, `MetricGrid` (utilizations+balances as percent bars, scalar metrics as stat cards, constraint-violations card turns red when >0, trip score badge), `WarningsPanel` (severity icon/color per code — red for unplaceable/empty-trip/time-limit, amber otherwise), `DeferredTable` (destination trip derived in-UI from placements → "→ Trip N" / "not placed"), `UnplaceableTable` (reason chip + detail, "All cargo was placed. ✓" empty state). Wired both shared components onto `ScreenSimulation`'s left panel. 308 tests green (25 new).
- Files: src/components/report/{reportView.ts(+test),ScoreBadge,MetricGrid,WarningsPanel,DeferredTable,UnplaceableTable,ExportButtons}.tsx, src/components/simulation/{TripSelector,ShopLegend}.tsx(+tests), src/components/screens/{ScreenReport(+test),ScreenSimulation}.tsx, src/App.test.tsx, src/utils/format.ts, docs/TASKS.md
- Decisions/deviations:
  - **T08's `ReportModel` adopted as-is** (per prompt): it carries no cargo *template* names, so the deferred/unplaceable tables resolve names in the UI (`buildTemplateNames` from the scenario — a lookup, not math). Destination-trip is likewise derived in-UI (`buildPlacementTripNumbers`), which the prompt explicitly calls for.
  - Added `fmtPctSafe` to `src/utils/format.ts` (T10's file, additive) — dashes non-finite ratios so a divide-by-zero balance can never render "NaN%". Every report percent goes through it.
  - Warnings panel on the selected-trip view shows result-level warnings + the selected trip's warnings merged.
  - `ShopLegend` lives on the Simulation screen (its filter drives the 3D scene); the Report screen uses `TripSelector` only (a shopFilter toggle would have no visible effect there).
  - Metric label wording is concise but maps 1:1 to idea.md's display list (e.g. "Blocked cargo" ← "Blocked cargo count", "Extra unloading moves" ← "Estimated extra unloading movements", "Trip score" ← per-trip "Overall score"). Checklist in the PR body.
  - Replaced `ScreenSimulation`'s old text caption with the TripSelector+ShopLegend panel; updated the one App-test assertion that keyed on the removed "placement(s)" text.
  - Verified end-to-end with Playwright against a real optimizer run (seed loadwise-1 → 2 trips): full display list renders, TripSelector switches trips, deferred rows show "→ Trip 2", Scenario JSON export downloads and `JSON.parse`s cleanly, zero console errors.
- Follow-ups: none blocking. T17 (demo mode) will drive this screen with the fixed demo seed.

## [2026-07-14 23:30] T15 — Delivery simulation
- Dev: daniel-dimitrov · Model: Fable 5 · Branch: feat/T15-delivery-simulation
- Done: stop-by-stop delivery simulation per the prompt's settled choreography. Pure route math in `src/three/Animations/deliveryTimeline.ts`: `buildRoutePlan` walks `trip.stops` maintaining the boxes still aboard, per stop derives unload order (distance-to-door asc, tiebreak id), distinct later-stop blockers via T06's `findBlockers` (T06 was merged — no stub needed), and the flat op list `move-blocker-out × B → deliver × D → return-blocker × B (reverse)`; `stopStateAt`/`stopDuration` map a per-stop clock onto door-open → highlight → ops → door-close → done (0.6 s phases). `plan.extraMovesTotal` uses the exact per-stop-distinct-set rule of T08's metrics, and a test pins `extraMovesTotal === metrics.extraUnloadingMoves`. `DeliveryAnimator.tsx` executes it in one `useFrame` (per-stop clock in shared `deliveryClock`; blockers slide to alternating staging slots outside the door and back, delivered items slide out and shrink away, shop-colour pulse on the stop's cargo, amber flash on blockers); doors are driven per frame from `deliveryClock.openDoors`. `DeliveryPanel.tsx` HUD: stop card (shop/colour/stop x/y/door/units), blocker callout, running extra-moves counter with route total, remaining stops, transport (play/pause, Next stop, Auto-play toggle, Restart route, speed, Exit) + per-stop progress bar. Blocking fixture variant `demoBlockingScenario`/`demoBlockingResult` (1 blocker, 1 extra move). Refactors: shared `dogLegAt` + `pulse.ts` reused by both animators; `useClockFraction` shared by both HUDs. 283 tests green.
- Files: src/three/Animations/{deliveryTimeline.ts(+test),useDeliveryTimeline.ts,DeliveryAnimator.tsx,pulse.ts,playbackClock.ts,loadingTimeline.ts,LoadingAnimator.tsx,index.ts}, src/three/VehicleScene/Doors.tsx, src/three/CargoLayer/CargoLayer.tsx, src/components/simulation/{DeliveryPanel.tsx(+test),PlaybackControls.tsx,useClockFraction.ts}, src/components/screens/ScreenSimulation.tsx, src/state/{uiStore.ts,stores.test.ts}, src/fixtures/demo.ts, .claude/skills/verify/SKILL.md, docs/TASKS.md
- Decisions/deviations:
  - **uiStore.Playback gained `autoPlay: boolean`** (additive; default false). Not a `src/types/` change, but it extends T09's playback shape — flagging here for the team.
  - **T14's CargoLayer unmount guard replaced**: verification caught that a store-resetting unmount cleanup misfires under React StrictMode's double-mount (entering the simulation screen with an active mode killed playback instantly). The reset now lives in `uiStore.goTo`/`setSelectedTrip` (navigate away or switch trip mid-playback → playback back to default); covered by store unit tests. Real user flows were never affected — only state injected before mount hit it.
  - Blockers exit through the door of the (first) item they block, not their own assigned door — that's the path they physically obstruct.
  - Optional camera pan toward the stop's door: skipped (prompt marked it optional-if-trivial; OrbitControls target lerp isn't trivial to do cleanly alongside user orbiting).
  - Stop choreography ending: manual mode pauses and waits for "Next stop"; Auto-play rolls on; turning Auto-play on while waiting also resumes.
  - Verified end-to-end with Playwright (blocking fixture injected via live store modules): full 3-stop route with blocker out/back and counter 1/1 matching the report metric, pause/restart/auto-play at 4×, truck empty at route end, leave-screen-mid-route lands back in idle, plus a real optimizer run (0 blockers) end-to-end. Deterministic mid-op freeze frames captured by writing `deliveryClock.t` directly.
- Follow-ups: camera pan to the active door (nice demo polish, cut-ladder adjacent). Blocker staging slots can visually overlap the swung-open rear door panels from some angles — cosmetic, revisit with T20 polish. T16's trip selector should use `setSelectedTrip` (already guards mid-playback switches).

## [2026-07-14 21:20] T14 — Loading animation
- Dev: daniel-dimitrov · Model: Fable 5 · Branch: feat/T14-loading-animation
- Done: loading-animation replay per the prompt's settled design. Pure timeline math in `src/three/Animations/loadingTimeline.ts` (STEP 0.6 / DUR 0.55, two-segment smoothstep dog-leg per item: staging outside the assigned door → doorway waypoint at final x/y (rear) or y/z (side) → exact final snap; `transformAt`/`buildItemPath`/`itemIndexAt` all unit-tested, incl. side-door staging and rear fallback for a missing door). `LoadingAnimator.tsx` is the single `useFrame` driver: advances the shared `loadingClock` by `delta × speed`, writes positions/visibility straight onto CargoBox meshes via a cargoId→mesh registry (new `registerMesh` prop on CargoBox), emissive pulse on the in-flight item, store writes only on discrete events (index change, auto-pause at end); unmount restores exact placed transforms. `PlaybackControls.tsx` overlay: Replay-loading entry, Play/Pause, Restart, speed cycle 0.5/1/2/4×, "item k / N" + thin progress bar (rAF-throttled read of the shared clock — no per-frame React state in the scene), Exit. Doors forced open while mode is `loading`. CargoLayer unmount guard resets playback to idle (covers trip switch, regenerate, leaving the screen). Side-door fixture variant `demoSideDoorScenario`/`demoSideDoorResult` added (shop-2 via left door). 256 tests green.
- Files: src/three/Animations/{loadingTimeline.ts(+test),playbackClock.ts,useLoadingTimeline.ts,LoadingAnimator.tsx,index.ts}, src/three/CargoLayer/{CargoBox,CargoLayer}.tsx, src/three/VehicleScene/Doors.tsx, src/components/simulation/PlaybackControls.tsx(+test), src/components/screens/ScreenSimulation.tsx, src/fixtures/demo.ts, .claude/skills/verify/SKILL.md, docs/TASKS.md
- Decisions/deviations:
  - Staging points/waypoints are computed in scene metres on mesh centres (not domain cm) — this module drives meshes directly and the cm→m conversion already happened in cargoModel/units; the 150 cm clearance constant is still expressed in cm through `m()`.
  - Rear waypoint z = itemDepth/2 ("just fully inside the door plane") — final z is always ≥ that, so segment 2 never reverses; same construction mirrored for side doors.
  - Timeline end auto-pauses but STAYS in loading mode (Restart/Play replay); Exit or leaving the screen returns to idle. Play on a finished timeline restarts.
  - The in-flight emissive pulse is cleared once per box on landing (tracked via `userData.loadingPulse`), never per-frame, so it can't stomp the React-driven selection highlight; selecting the currently-flying box can still lose its highlight until the next re-render — accepted edge case.
  - Track-scope touches outside three/: PlaybackControls + ScreenSimulation are Track C files but the prompt explicitly assigns them; `fixtures/demo.ts` (Track A) got the side-door variant the acceptance criteria demand.
  - Verified end-to-end with Playwright against the dev server (23-item optimizer run: pause freezes counter+bar, 4× completes, restart rewinds, leave+return lands idle; side-door fixture injected via live store modules shows the left-door flight). Recipe persisted in `.claude/skills/verify/SKILL.md`.
- Follow-ups: scrubbing stays out of scope (cut-ladder). Cargo labels read mirrored when viewed from behind through the box (drei Text is double-sided) — pre-existing T13 behaviour, cosmetic, maybe T20 polish. T15 can reuse the mesh registry + playbackClock pattern for delivery playback.

## [2026-07-14 01:00] T11 — Optimizer Web Worker + client
- Dev: daniel-dimitrov · Model: Fable 5 · Branch: feat/T11-optimizer-worker
- Done: `src/workers/optimizer.worker.ts` (Vite module worker running the real `optimize()`, progress throttled to ≤ ~10 msgs/s, errors mapped to `OptimizerError`) and `src/workers/optimizerClient.ts` (`createOptimizerClient` — requestId per run via `crypto.randomUUID`, stale responses ignored, cancel = `worker.terminate()` + `CancelledError` + lazy respawn, second `run()` auto-cancels the first, `worker.onerror` rejects and replaces the worker). Rewired `optimizationStore.run/cancel/reset` to the client, deleting the T09 setTimeout scaffold. 8 client unit tests + updated store/App/Planning tests; 172 tests green. Verified `vite build` emits a standalone worker chunk and `vite preview` serves it (curl-level check).
- Files: src/workers/optimizer.worker.ts, src/workers/optimizerClient.ts(+test), src/state/optimizationStore.ts, src/test/fakeOptimizerClient.ts, src/state/stores.test.ts, src/App.test.tsx, src/components/screens/ScreenPlanning.test.tsx, docs/TASKS.md, docs/prompts/T11-optimizer-worker.md
- Decisions/deviations:
  - **Mock phase skipped**: T07 was already merged when this task started, so the worker wires the real `optimize` directly — no `mockOptimize.ts` was ever needed. Prompt file annotated. Effectively Phase 1+2 landed together; **end-to-end optimize is now live (Milestone M2) — Track A please sanity-check real seeds through the UI.**
  - `createOptimizerClient(spawn?)` takes an injectable worker factory — this is the "message handling factored pure" requirement (plus an exported pure `handleWorkerMessage`); tests drive the full protocol without a real Worker.
  - Store keeps a `status !== 'running'` guard in then/catch replacing the old runToken: a settle racing a same-tick cancel/reset must not overwrite state.
  - jsdom suites inject `src/test/fakeOptimizerClient.ts` via new `setOptimizerClient()` seam (jsdom has no Worker); UI test timer flushes became `runAllTimersAsync` since the client settles through microtasks.
  - Manual browser click-through (real progress bar, cancel mid-run, rapid double-run) still owed a human pass — noted in the PR; T19 Playwright will automate it.
- Follow-ups: worker progress percent is coarse for tiny scenarios (optimize emits ~1 msg/trip); fine for demo sizes. Store could expose `elapsedMs` in the progress UI later (T16 shows it in the report).
- Addendum (same session, post-review): closed three coverage gaps on this branch — store error path (worker failure → `status: 'error'` + retry recovers), unit tests for `planning/totals.ts` (chips grouping, zero-cargo, weight-vs-volume ratio independence incl. > 1), and TotalsBar over-capacity render (amber + multi-trip hint). The totals tests strictly belong to T10's scope but ride this branch. 180 tests green.

## [2026-07-14 00:30] T10 — Scenario Setup & Planning screens
- Dev: daniel-dimitrov · Model: Fable 5 · Branch: feat/T10-setup-planning-screens
- Done: real Setup screen (3 vehicle cards with proportional SVG side-view schematics, side-door radio group with top-view diagrams, shop-count slider 3–8, seed field + randomize + helper text, disabled "Load demo" placeholder) and Planning View (header with vehicle summary + copyable seed badge, shop cards in delivery order with color dot / type badge / stop number / door chip / template × count chips / per-shop weight+volume, totals bar with weight & volume capacity bars that go amber past 100%, Optimize with progress bar + Cancel, inline error alert with Retry, Regenerate and Back). Added `src/utils/format.ts` + RTL tests for both screens; 160 tests green.
- Files: src/utils/format.ts(+test), src/components/setup/{VehiclePicker,SideDoorPicker,ShopCountField,SeedField}.tsx, src/components/planning/{ShopCard,TotalsBar,SeedBadge}.tsx, src/components/planning/totals.ts, src/components/screens/ScreenSetup.tsx(+test), src/components/screens/ScreenPlanning.tsx(+test), src/App.test.tsx, docs/TASKS.md, docs/prompts/T10-setup-and-planning-screens.md
- Decisions/deviations:
  - format.ts grew `fmtM3(cm3)` and `fmtDims(dims)` beyond the three helpers the prompt named — needed for volume totals and W×H×D lines; prompt file updated to stay truthful. T16 can reuse all five.
  - Auto-advance to Simulation happens only for runs *started from Planning* (local `awaitingRun` flag watching store status) — revisiting Planning with an existing result must not bounce the user back to Simulation.
  - Regenerate = `randomizeSeed() → generate() → goTo('planning')`: `generate()` intentionally resets the UI to setup (new-scenario semantics from T09), so Planning re-navigates onto itself.
  - Planning with no scenario renders an empty state that still contains a disabled Optimize button (the prompt's test spec requires "optimize disabled when no scenario", not a hidden one).
  - Touched T09's `src/App.test.tsx` (Track C, same track): the shell happy path changed because Generate now navigates straight to Planning and a finished run auto-advances to Simulation.
  - Seed copy uses `navigator.clipboard?.writeText` with silent fallback — jsdom/insecure contexts have no clipboard, and the seed is visible on the badge anyway.
- Follow-ups: T11 should set `progress.percent`/`stage` for a real progress bar (UI already binds both) and populate `error` (alert + Retry are wired but only reachable via store state today). T17 activates the "Load demo" placeholder button on Setup.
